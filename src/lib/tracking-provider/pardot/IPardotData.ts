export interface IPardotProviderOptions {
  /**
   * The client that is used to register the api
   */
  clientId: string;

  /**
   * The application that is used to register the api
   */
  applicationId: string;

  /**
   * The hostname that is used to register the api
   */
  hostname: string;
}
